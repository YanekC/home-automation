- name: Create directory
  ansible.builtin.file:
    path: "{{ home_path_actual_budget }}"
    state: directory
- name: Copy docker compose file to pi
  ansible.builtin.template:
    src: ../files/compose.yml.j2
    dest: "{{ home_path_actual_budget }}/compose.yml"
    force: true

- name: Copy config file
  ansible.builtin.copy:
    src: ../files/config.json
    dest: "{{ gros_disque_path_actual_budget }}/"
    force: true

- name: Tear down hass services
  community.docker.docker_compose_v2:
    project_src: "{{ home_path_actual_budget }}"
    state: absent

- name: Create and start hass services
  community.docker.docker_compose_v2:
    project_src: "{{ home_path_actual_budget }}"
    pull: always
