---
- hosts: all
  become: yes

  vars:
    gros_disque_path: /mnt/gros_disque/docmost
    grosse_usb_path: /mnt/grosse_usb/docmost

    home_path_docmost: /home/yanck/docmost

  tasks:
    - name: Install Packages
      include_role:
        name: install_packages
    - name: Basic Conf
      include_role:
        name: basic_conf

    - name: Setup docmost
      include_role:
        name: setup_docmost
